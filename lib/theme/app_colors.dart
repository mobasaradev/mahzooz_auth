import 'package:flutter/material.dart';

class AppColors {
  AppColors._();
  // Primary Colors
  static const white = Colors.white;
  static const black = Colors.black;
  static const grey = Colors.grey;
  static const primary = Color(0Xff03AAE0);

  // Text colors
  static const tiredTextColor = Color(0Xff55545a);
}
